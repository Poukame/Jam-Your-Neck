import Header from '../src/Components/Header';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event'

describe('when the header is displayed user', () => {
    test('should be able to toggle the theme', () => {
        ///
    })

    test('should be able to install the WPA when clicking the  install button', () => {
        ///
    })
})

